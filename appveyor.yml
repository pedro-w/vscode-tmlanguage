version: 1.0.{build}
environment:
  nodejs_version: "LTS"
  NODE_SKIP_PLATFORM_CHECK: 1
platform: x64
install:
- ps: Install-Product node $env:nodejs_version
- npm install -g @vscode/vsce
- npm install
build_script:
- vsce package
test_script:
- npm test
